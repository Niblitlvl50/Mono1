
language: cpp
sudo: required
dist: trusty

matrix:
  include:

#  - os: linux
#    compiler: gcc
#    env: COMPILER=g++-4.9
#    addons:
#      apt:
#        sources: 
#          - ubuntu-toolchain-r-test
#        packages:
#          - g++-4.9
#          - libsdl2-dev

  - os: linux
    compiler: clang
    env: COMPILER=clang++-7.0
    addons:
      apt:
        sources:
          - ubuntu-toolchain-r-test
        packages:
          - libsdl2-dev
          - libopenal-dev

script:

  - cmake -DCMAKE_CXX_COMPILER=$COMPILER . && make